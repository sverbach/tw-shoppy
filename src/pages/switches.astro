---
import { KeySwitchSection } from '@/components/KeySwitchSection';
import BaseLayout from '../layouts/BaseLayout.astro';
import { setCache } from '../utils/cache';
import { PageTitle } from '@/components/PageTitle';

const title = 'Switches';
const ip = Astro.clientAddress;

setCache.short(Astro);
---

<BaseLayout title={title}>
  <div class="flex max-w-7xl flex-col rounded-3xl border-2 p-5 px-7 shadow-md">
    <PageTitle titleText="Switches" client:load />
    <div class="flex flex-col gap-6">
      <KeySwitchSection buyerIP={ip} client:load />
    </div>
  </div>
</BaseLayout>
